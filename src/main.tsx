import React from "react";
import ReactDOM from "react-dom/client";
import PharmacologyQuizApp from "./PharmacologyQuiz";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <PharmacologyQuizApp />
  </React.StrictMode>,
);
